# MathWeaver 教学正式版发布记录（2026-08-28）

## 发布结论

教学版已经以独立侧车方式部署到正式服务器，原有 `3000/5001` 服务未修改。当前正式数据包含一个“凸优化”班级、一份不可变教学快照、90 个节点身份和 90 个快照节点映射；教师、学生、班级、图谱快照、作业、进度、提交、人工评分和成绩发布闭环已通过隔离验收。

核心运行代码基线为 `82eb88055140`，包含以下生产修复：

- 严格外键数据库中，先写入作业父记录，再写入评测节点；
- 切换发布软链接后显式重启已运行的两个侧车服务；
- 仅给 `nginx` 服务账号授予根目录和当前发布目录的穿越权限，不开放目录枚举。

## 服务拓扑

| 入口 | 地址 | 说明 |
| --- | --- | --- |
| 对外 Web | `http://101.132.100.225:18080` | Nginx 侧车入口 |
| 教学后端 | `127.0.0.1:5002` | Gunicorn，仅本机监听 |
| 教学前端 | `127.0.0.1:5174` | React Router Serve，仅本机监听 |
| 原有服务 | `3000/5001` | 保持运行且未被发布脚本修改 |

Systemd 服务为 `mathweaver-teaching-backend.service` 和 `mathweaver-teaching-frontend.service`。发布目录遵循 `/opt/mathweaver/releases/<git-sha>`，`/opt/mathweaver/current-teaching` 为原子切换软链接。

## 数据库与备份

- 正式数据库：MySQL `mathweaver`；
- Alembic 当前版本：`20260828_03`；
- 当前表数：30；本次教学域新增 20 张表、41 条外键；
- 上线前备份：`/opt/mathweaver/backups/20260828-164455`；
- 环境文件：`/opt/mathweaver/.env.teaching`，权限 `600`；
- 迁移前已有用户、登录会话和用户设置计数已核对，迁移未清空原数据。

备份目录包含压缩 SQL、代码与服务配置、迁移前计数、离线迁移 SQL 和 SHA-256 清单。回滚应用版本时不自动执行数据库 downgrade；只有出现不兼容数据问题时，才按停机恢复流程使用该备份。

## 正式图谱数据

数据源位于 `backend/seeds/convex_optimization`，由负责人交付的 Markdown、节点 JSON 和边 JSON 归一化生成。

| 对象 | 正式结果 |
| --- | --- |
| 班级 | 1 个，标题“凸优化” |
| 历史图谱 | 1 个 |
| 教学快照 | 1 个 |
| 节点 / 边 | 90 / 226 |
| 节点身份 / 快照映射 | 90 / 90 |

固定业务标识：

- 班级：`ac4fe69c39bb5d60b24bf4501aa6541b`
- 历史图谱：`ea3e236dc163510da3dea0d75b7a6d00`
- 教学快照：`7bfb3a8f63af5864b50a1adeb2de9e84`

源文件 SHA-256：

- Markdown：`4f0ee13b9410e5751431a10e1607880caaefcd59150d6fa7b0b89d1a27dcd908`
- 节点 JSON：`a0b6a63e86faf0b80af481d4920c4bd9832f4cfe96d669088495385351dc51b6`
- 边 JSON：`3993d48cccf5b9b9cf97b5749e533f4178ffb6301c2abcdda3f91cfa687edf30`

正式导入已重复执行并确认幂等：班级、历史图谱和快照 ID 不变，节点身份及映射计数不增加。

## 验收证据

- 严格外键问题已在 SQLite 开启 `PRAGMA foreign_keys=ON` 后稳定复现，并由自动化回归测试覆盖；
- 隔离端到端验收：48 个 HTTP 步骤、27 项业务断言全部通过；
- 覆盖教师/学生鉴权、班级加入、移除与恢复、快照幂等、权限拒绝、确定性路径、免测发布、学习进度、作业提交、人工评分和成绩发布；
- 验收期间 AI 日额度临时设为 0，未发起外部模型请求；结束后完整恢复原环境文件；
- 临时班级、快照、作业、教师和学生账号残留计数均为 0；
- 正式班级、历史图谱、快照、节点身份和节点映射验收前后分别保持 `1/1/1/90/90`；
- 服务器证据：`/opt/mathweaver/backups/20260828-164455/e2e-acceptance-20260828T101720Z-1db42a.json`；
- 已执行一次回滚到 `896a72b864a5fda5001e750e608d36c8cf126d8d`，确认服务就绪后再前滚到新版本；
- 本机及公网 `/api/v2/ready` 均返回数据库就绪。

## 回滚方法

```bash
sudo bash /opt/mathweaver/current-teaching/scripts/deploy_teaching_release.sh rollback
```

回滚只切换 `current-teaching` 并重启两个教学侧车，不触碰原有服务，也不执行数据库降级。回滚后必须再次检查本机和公网 `/api/v2/ready`。

## 已知限制与后续事项

- 当前对外入口只有 HTTP 和端口 `18080`；正式域名、TLS 证书及 80/443 入口仍需负责人提供或确认；
- 图谱校验仍保留负责人数据中的只读告警：8 个旧版全局 ID、27/90 的连续原文映射覆盖、225/226 的定义依赖覆盖；
- 前端构建存在大于 500 kB 的 chunk 提示，不影响本次上线，但后续可做按路由拆包；
- SSH 仍使用现有 root 密码登录。配置 SSH 密钥、轮换密码和关闭 root 密码登录需要单独维护窗口，未在本次发布中擅自执行。
