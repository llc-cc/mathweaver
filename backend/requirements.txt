# ── Web API ──────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pyinstaller>=6.0.0
python-multipart>=0.0.9
pypdf>=5.6.0

# ── LLM / Pipeline core ──────────────────────────────────────────────
openai>=1.0.0          # SimpleLLM uses openai-compatible client
requests>=2.31.0
python-dotenv>=1.2.0
numpy>=1.24.0
sympy>=1.12             # restricted scalar verification backend for MatrixFlow
tqdm>=4.0.0
dashscope>=1.0.0       # Qwen / DashScope embedding models

# ── Utilities ────────────────────────────────────────────────────────
aiohttp>=3.9.0         # async HTTP used by some integrations
pylatexenc>=2.10       # TeX source parsing for structured statement extraction
